let imgBig = document.getElementById('full-img')

// let startX = 0

// imgBig.addEventListener('touchstart', function(e) {
// 	if(e.touches.length === 1) {
// 		startX = e.touches[0].clientX
// 	}
// })

// imgBig.addEventListener('touchend', function(e) {
// 	console.log(e)
// 	if(e.touches.length === 1) {
// 		console.log(e.touches[0].clientX)
// 		if (e.touches[0].clientX - startX > 100) console.log('swipe!');
// 	}
// })


// imgBig.addEventListener('touchstart', function(e) {
// 	if(e.touches.length != 1 || started) {
// 		return
// 	}
// }

